// lib/config/ui_config.dart

/// Config basique pour partager le thème et la langue
/// entre LoginPage et RegisterPage.
class UIConfig {
  static bool isDarkMode = true;   // par défaut: mode sombre
  static bool isFrench = true;     // par défaut: français
}
